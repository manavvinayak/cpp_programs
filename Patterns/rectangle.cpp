#include <iostream>
using namespace std;
int main() {
    // take input from user ,no. of rows and no. of columns
    // basic code using fixed column number  
    int n;
    cout<<"No. of rows: ";
    cin>>n;
    for(int i=1;i<=n;i++){
        cout<<"***"<<endl;
    }
    // for taking both input of rows and columns ,nested loops will be used  
}