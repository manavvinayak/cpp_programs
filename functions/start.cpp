#include <iostream>
using namespace std;
int sum(int t,int s) {
     int add = t + s;
    return add;
    // return keyword is used to end with the datatype values user provided

}
// main function always starts at beginning
int main() {
    // sum(4,5);
    cout<<sum(4,5);
    return 0;
}

